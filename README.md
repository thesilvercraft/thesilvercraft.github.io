# SilverCraft
This website is built using Jekyll. 
* **Edit Content:** Update `_data/projects.yml` to add or modify project info.
* **CI/CD:** Automatically deployed via GitHub Actions (see `.github/workflows/deploy.yml`).
